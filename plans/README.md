# Implementation Plans

Generated by the improve skill on 2026-08-19 from an audit of the OpenAISF
reference implementation at commit `9424806`. Execute in the order below unless
dependency notes say otherwise. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Make the package version report the live standard (1.0.0a2) | P1 | S | — | DONE |
| 002  | Turn CLI config errors into clean messages on exit 2 | P1 | S | — | DONE |
| 003  | Give the MCP check tool a keyring so T3/T4 evidence can verify | P1 | M | — | DONE |
| 004  | Align the README's published figures with the pinned tests | P2 | S | — | DONE |
| 005  | Add a CI pipeline that runs the two release gates | P1 | M | 001 | DONE |
| 006  | Ignore generated build artifacts | P3 | S | — | DONE |
| 007  | Fix the `EvidenceRecord.age` return annotation | P3 | S | — | DONE |
| 008  | Detect fabricated decisions as a disqualifying contradiction | P3 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 005 (CI) depends on 001 because the CI pipeline runs the full test suite,
  including the new `tests/test_version_consistency.py` added by 001. Run CI
  only after 001 is DONE, or the pipeline fails on a test that does not exist
  yet.
- 002, 003, 004, 006, 007, 008 touch disjoint files and can run in any order
  relative to each other and to 001. 003 and 007 both touch
  `src/openaisf/evidence.py` (different lines: 86-88 vs the load path); if run
  in parallel worktrees, rebase 003 onto 007 or vice versa — trivial conflict.

## Findings considered and rejected

- No rejected findings. All eight audit findings are planned. The following
  were noted during audit but deliberately not planned:
  - The changelog's v1.0 table row `Tests | none | 170` is a historical
    statement about the v1.0.0a1 release and is left untouched (plan 004
    corrects only current-state README figures).
  - `tools/adapters/gateway_adapter.py` reports `VERSION = "1.0.0"`; that is the
    adapter's own producer version written into evidence records, not the
    package version, and is out of scope for plan 001.
  - The MCP `SUPPORTED_VERSIONS` are wire-protocol versions, not package
    versions, and must not be changed by plan 001.